name: CI and Release

on:
  push:
    branches:
      - main
      - next
      - next-major
      - beta
      - alpha
      - '*.x'     # Matches branches like '1.x', '2.x'
  pull_request: # Run on all PRs regardless of target branch
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Job 1: Commit Linting
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history to check commit differences

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies
        run: npm ci

      - name: Validate all commits
        run: npx commitlint --from ${{ github.event.pull_request.base.sha || github.event.before }} --to ${{ github.event.pull_request.head.sha || github.sha }} --verbose

  # Job 2a: Static Checks (Linting)
  static-checks:
    needs: [commitlint]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies
        run: npm ci

      # since there are lots of lint errors, run lint with warnonly to not fail the build
      - name: Run linting
        run: npm run lint:warnonly

  # Job 2b: Tests
  run-tests:
    runs-on: ubuntu-latest
    needs: [commitlint]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies
        run: npm ci

      - name: Run regression tests with code coverage
        run: npm run test:coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: workbench-frontend-coverage
          path: coverage
          retention-days: 7

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: center-for-threat-informed-defense/attack-workbench-frontend
          files: coverage/lcov.info
          verbose: true

      - name: Verify integrity of dependencies
        run: npm audit signatures

  # Job 3: Semantic Release and Docker Build
  release:
    needs: [static-checks, run-tests]
    # Only run on pushes (not PRs) - semantic-release will determine whether to release
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write         # To publish a GitHub release
      packages: write         # To publish to GitHub Package registry
      issues: write           # To comment on released issues
      pull-requests: write    # To comment on released pull requests
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true # Sets up docker build command as an alias to docker buildx
          driver-opts: image=moby/buildkit:latest
          platforms: linux/amd64,linux/arm64

      - name: Install dependencies
        run: npm ci

      - name: Semantic Release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # needed to authenticate with @codedependant/semantic-release-docker
          DOCKER_REGISTRY_USER: ${{ github.repository_owner }}
          DOCKER_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          # Temporarily disables all Git hooks
          HUSKY: 0
        run: npx semantic-release